{
  "name": "introspect_providers_returns_object",
  "description": "Introspect{providers} response is a non-null object.",
  "test": { "op": "introspect", "args": { "subject": "providers" } },
  "expect": {
    "status": "ok",
    "data_type": "object"
  }
}
