{
  "name": "introspect_config_returns_object",
  "description": "Introspect{config} response is a non-null object.",
  "test": { "op": "introspect", "args": { "subject": "config" } },
  "expect": {
    "status": "ok",
    "data_type": "object"
  }
}
