{
  "name": "introspect_backoff_returns_object",
  "description": "Introspect{backoff} response is a non-null object.",
  "test": { "op": "introspect", "args": { "subject": "backoff" } },
  "expect": {
    "status": "ok",
    "data_type": "object"
  }
}
