{
  "name": "resolve_basename_filter",
  "description": "The basename filter reduces an env-sourced path value to its final component.",
  "virtual": { "filters.based": "env.PYVAR | basename" },
  "env": { "PYVAR": "/foo/bar/baz" },
  "test": { "op": "resolve", "args": { "key": "filters.based" } },
  "expect": { "status": "hit", "data_type": "string", "data_equals": "baz" }
}
