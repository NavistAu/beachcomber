{
  "name": "resolve_cascade_miss_yields_empty_string",
  "description": "env.X or cache.P.F cascade yields empty string, not an error, when both terms miss.",
  "virtual": { "myapp.workspace": "env.MYAPP_ENV or cache.myappcache.workspace" },
  "test": { "op": "resolve", "args": { "key": "myapp.workspace" } },
  "expect": { "status": "hit", "data_type": "string", "data_equals": "" }
}
